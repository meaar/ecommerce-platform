# Frontend

Frontend Angular 22 da loja.

## Scripts

```powershell
npm install
npm start
npm run build
```

A API usada localmente e configurada em `src/environments/environment.ts` como `http://localhost:3000/api`. Em producao, altere essa URL para o endereco publico do backend.
